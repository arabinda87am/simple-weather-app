const { OpenWeatherApi } = require("../services/OpenWeatherApi");
const { assert } = require("chai");

describe("OpenWeatherApi Unit Testing", () => {
    it("testing getCurrentWeatherByCoordinates ", () => {

    });
});